---
name: velog-publisher
description: "Draft and auto-publish a game-dev devlog to 디땁's velog blog at the end of each work session, sourced only from his project's devlog.md file, via browser automation on his linked computer."
---

# Velog 주간 개발 회고 자동 발행

## 배경 / 목적
디땁(muggie1379)은 반도체 자동화 SW에서 게임업계(개발 트랙)로 이직을 준비 중이며, 유니티 클라이언트 + 게임 서버 사이드 프로젝트를 포트폴리오로 만들고 있다. 그 진행 과정을 velog(https://velog.io/@muggie1379/posts)에 매주 개발 회고로 자동 발행해 포트폴리오 겸 이직 어필 자료로 쓰려는 목적.

**중요: 이 블로그는 이직 포트폴리오 용도다.** 내용은 반드시 실제 진행 상황에 근거해야 하며, 확인되지 않은 내용을 지어내서는 안 된다. devlog.md에 없는 내용은 절대 추가하지 않는다.

## 사용자가 확정한 방식 (변경 시 이 파일을 업데이트할 것)
- 발행 방식: **완전 자동 발행** — 초안 작성과 게시 버튼 클릭 자체는 AI가 하지만, 실제로 게시하기 직전에 오늘 한 일 요약/잔건/다음 계획을 사용자에게 보여주고 확인을 받는다 (아래 "작업 마무리 루틴" 2번 참고). "검수 없음"이 아니라 "검수를 그 한 번의 확인으로 최소화함"이다.
- 주기: 데일리/위클리로 나누지 않고, **작업을 마무리할 때마다 매번** 아래 "작업 마무리 루틴"을 수행한다.
- 내용 소스: git 커밋 로그를 직접 파싱하지 않는다 (연결된 기기에 shell/git 실행 도구가 없어 불안정함). 대신 프로젝트 루트의 **devlog.md** 파일에 사용자가 그날 작업 내용을 짧게 적어두는 방식을 채택.

## 전제 조건 (실행 전 반드시 확인)
1. 게임 프로젝트 저장소 폴더가 사용자 컴퓨터에 존재하고, 이 세션/스케줄 작업에 `device_request_folder_access`로 연결되어 있어야 함 (연결 안 돼 있으면 이 단계에서 멈추고 사용자에게 요청)
2. 그 폴더 루트에 `devlog.md`가 있고, 마지막 발행 이후 새로 추가된 내용이 있어야 함. 새 내용이 전혀 없으면 억지로 지어서 포스팅하지 말고, 이번 주는 스킵하거나 사용자에게 알린다.
3. 연결된 컴퓨터의 브라우저가 velog.io에 로그인된 상태여야 함 (로그인 안 되어 있으면 발행 실패 — 사용자에게 알림)

## 절차
1. `device_list_dir`로 devlog.md 파일 확인 → `device_stage_files`로 스테이징해서 Read로 내용 확인
2. 마지막 발행 이후 추가된 부분만 골라서, 그 내용만 근거로 개발 회고 포스트 초안 작성 (제목 + 본문). 과장하거나 없는 내용을 추가하지 않는다. 아래 "가독성 원칙"을 반드시 적용한다.
3. `mcp__remote-devices__Claude_Browser__*` 도구로 velog.io 글쓰기 에디터를 열고, 제목/본문을 입력
4. 발행(포스트 등록) 버튼 클릭
5. 발행 완료 후 devlog.md에서 이번에 반영한 부분을 표시(예: `<!-- published 2026-XX-XX -->` 주석 추가)해서 다음 주에 중복 발행되지 않게 한다

## 가독성 원칙 (사용자 지시, 2026-09-13)
글이 줄글(prose)로만 이어지면 읽기 힘들다는 피드백을 받았다. 포스트를 작성할 때 항상 시각적 구조를 고려한다:
- 여러 항목을 나열할 때는 문단 대신 **리스트(불릿/번호)** 를 쓴다.
- 항목 간 비교나 스펙 정리(예: 기술 스택, 이번 주 작업 항목별 상태)에는 **표**를 쓴다.
- 수치 추이(예: 성능/부하 테스트 결과)가 있으면 표나 간단한 텍스트 그래프 등으로 시각화하는 것을 고려한다.
- 소제목(`##`)으로 섹션을 나눠서 한 눈에 구조가 보이게 한다.
- 다만 리스트/표를 위해 내용을 억지로 쪼개거나 과장하지 않는다 — devlog.md에 근거한 사실만, 더 읽기 쉬운 형태로 정리하는 것이 목적이다.

## 스크린샷 원칙 (사용자 지시, 2026-09-13)
포스트를 처음 보는 사람 — 특히 면접관, 비전공자, 학생 — 도 하나씩 따라올 수 있도록, 글이 허용하는 한 스크린샷을 적극적으로 많이 첨부한다. 글만으로 설명하고 넘어가지 말고, 단계마다 그 화면을 보여준다.

**스크린샷 확보 방법:**
- Claude Code는 별도 프로그램 설치 없이 PowerShell(.NET `System.Drawing` + `System.Windows.Forms`)로 전체 화면을 직접 캡처할 수 있다는 걸 확인함 (Snipping Tool 등 추가 설치 불필요).
- 사용할 스크린샷은 Claude가 직접 캡처하든 사용자가 수동으로 찍든 관계없이 **모두 `E:\screenshot` 폴더에 저장**한다. 포스트에 쓸 이미지를 찾을 때는 이 폴더를 먼저 확인한다.
- 다만 전체 화면 캡처에는 무관한 창(파일 탐색기, 다른 브라우저 탭, 북마크바 등 개인정보)이 같이 찍히므로, 블로그에 올리기 전 관련 창/영역만 크롭해서 사용한다.
- Claude가 직접 화면을 캡처하기 전에는 반드시 사용자에게 먼저 안내한다 (동의 없이 조용히 캡처하지 않음).
- Unity Editor처럼 Claude가 클릭/입력으로 조작할 수 없는 데스크톱 앱은, 사용자에게 어떤 동작을 해달라고 미리 요청한 뒤 그 결과 화면을 캡처한다. 브라우저(velog 글쓰기 화면 등)는 Claude가 직접 조작하며 스크린샷도 그 과정에서 자연스럽게 확보한다.
- 작업을 다시 해봐야 하는 상황이 생기면, 그 재작업 과정에서도 스크린샷을 하나씩 다시 찍어 기록한다.

**메타 소재 — Velog 자동화 Skill 자체를 다루는 devlog:** Claude Code에 이 `velog-publisher` Skill을 만들고 자동 발행을 설정한 과정 자체도 "AI 활용/업무 자동화" 어필 포인트로서 별도 devlog 소재로 다룰 가치가 있다. 이 주제를 다룰 때는 Skill 생성 화면, SKILL.md 내용, 트리거 방식 등을 스크린샷과 함께 처음 보는 사람도 이해할 수 있을 만큼 자세히 설명한다.

## 서술 순서 원칙 (사용자 지시, 2026-09-13)
실제 작업은 시행착오·되돌리기·순서 꼬임이 있을 수 있다 (예: 뭔가 설치했다가 실제로는 필요 없어서 되돌리거나, 에러가 나서 다시 시도하는 등). 하지만 devlog에는 이 과정을 시간순 그대로 나열하지 않는다. 대신 "지금 아는 걸 알고 처음부터 다시 한다면 어떤 순서로 했을까"를 기준으로, 깔끔하고 논리적인 순서의 튜토리얼처럼 재구성해서 쓴다.

- 이것은 내용을 미화하거나 안 한 일을 했다고 쓰는 것과 다르다 — 실제로 했던 작업과 그 결과는 그대로, 정확하게 담되, **제시 순서와 구성**만 독자가 따라가기 좋게 정리하는 것이다.
- 막다른 길로 갔다가 되돌린 시행착오 자체는 굳이 나열하지 않아도 되지만, 과정에서 얻은 유의미한 교훈(예: "SVN 작업 복사본은 파일을 직접 옮기면 깨지니 반드시 svn move/delete를 써야 한다")은 독자에게 도움이 되므로 남긴다.
- 이 원칙은 이번 포스트뿐 아니라 앞으로 작성하는 모든 devlog에 기본으로 적용한다.

## 발행물 정리 원칙 — 시리즈 구분 + Skill 소개 글 (사용자 지시, 2026-09-13)
- velog의 **시리즈** 기능으로 포스트 종류를 구분한다. 현재 3개:
  - **"DevLog"** — 프로젝트의 실제 진행 스토리(기획부터 환경설정, 개발까지 순서대로). 이 Skill이 주로 다루는 시리즈.
  - **"Workflow"** — 그 외 독립적인 참고 자료(이 Skill 소개처럼 그때그때 갱신되는 글).
  - **"Velog"** — 블로그 자체에 대한 메타 글(예: "블로그 개요" — 블로그 목적/소개). 사용자가 직접 관리하며, 이 Skill이 자동으로 글을 추가하는 대상은 아니다.
- 이 Skill(`velog-publisher`) 자체의 내용을 그대로 담은 소개 포스트를 velog에 하나 유지한다. 이 SKILL.md가 바뀔 때마다 새 글을 쓰지 않고, **기존 글을 수정**해서 본문에 "마지막 업데이트: YYYY-MM-DD"를 최신화한다.
- 참고: 이 Skill은 Claude Code가 아니라 일반 Claude 채팅에서 처음 만들어졌다. 그래서 Claude Code 세션 히스토리 검색으로는 최초 생성 프롬프트를 찾을 수 없다 — 소개 포스트에는 지금 시점의 실제 파일 내용을 근거로 쓰고, 최초 생성 경위를 추측해서 쓰지 않는다.

## History 게시글 구조 (사용자 지시, 2026-09-13)
devlog.md는 날짜(`## YYYY-MM-DD`)를 큰 틀로 유지하고, 그 날짜 안에서 실제로
있었던 카테고리만 `###` 소제목으로 나눈다 (없는 카테고리는 그날 생략). 지금까지
쓰인 카테고리: 서버, 클라이언트, 인프라/자동화, Velog 자동화 — 새로운 종류의
작업이 생기면 카테고리를 추가한다. History 게시글(Workflow 시리즈)은 이
devlog.md를 그대로 미러링하므로, devlog.md를 이 구조로 써두면 History 갱신이
그대로 복사-반영만 하면 되게 쉬워진다.

## DevLog 제목 형식 및 목차 (사용자 지시, 2026-09-13)
제목은 `DevLog #N (YYYY-MM-DD) : 부제` 형식으로 짧게 쓴다 (예: `DevLog #0 (2026-09-13) : 기획 및 로드맵`). 부제는 그 글의 핵심 내용을 몇 단어로 압축한다.

DevLog 번호는 주차가 아니라 **주제/단계** 단위로 매긴다. 현재까지의 목차:
- **#0** — 기획 및 로드맵 (발행 완료). 개발 착수 전 순수 기획: DotSoccer 게임 규칙, 서버 아키텍처 방향, 기술 스택, 로드맵.
- **#1** — 환경설정. Chrome 확장(Claude in Chrome) 설치, Unity MCP 설치 등 개발 환경을 처음부터 갖추는 과정을 스크린샷과 함께 누구나 따라할 수 있도록 자세히 다룬다. 아직 실제 게임/서버 코드 개발은 다루지 않는다.
- **#2 이후** — 본격적인 개발. DotSoccer 로컬 프로토타입 재구현부터 서버 구현, 클라이언트-서버 연결 등 실제 개발 진행상황을 순서대로 담는다.

새로운 주제가 시작될 때마다 목차에 항목을 추가하고, 이 표를 최신 상태로 유지한다.

## 작업 마무리 루틴 (사용자 지시, 2026-09-13 / 검수 단계 추가 2026-09-13)
데일리/위클리로 나누지 않는다 — **작업을 마무리할 때마다 매번** 아래를 수행한다:
1. 그날 실제로 진행한 내용을 `devlog.md`에 사실 그대로 짧게 기록한다 (위 "History 게시글 구조"의 카테고리 소제목 형식으로).
2. **velog에 실제로 게시/발행하기 전에, 오늘 한 일 요약 + 아직 안 끝난 잔건 + 다음에 할 계획을 사용자에게 먼저 보여주고 확인을 받는다.** "완전 자동 발행"은 이 확인 뒤에 실제 게시 버튼을 누르는 단계부터를 말하는 것이지, 확인 자체를 생략해도 된다는 뜻이 아니다. 사용자가 내용을 고치라고 하면 반영한 뒤 다시 확인받는다.
3. 확인을 받으면, 그 내용이 현재 진행 중인 DevLog 주제(#N)에 해당하는 경우 새 글을 만들지 않고 **해당 velog 게시글을 이어서 수정**해 그 시점까지의 내용을 반영한다. 주제가 완전히 마무리됐고 다음 주제로 넘어갈 때만 새 DevLog 번호로 새 글을 발행한다.
4. Workflow 시리즈에 해당하는 독립 자료(예: Skill 소개, History)가 바뀌었다면 그 글도 같이 최신화한다.

## Claude Code에서 실행할 때 도구 매핑 (실전 확인, 2026-09-13)
이 Skill은 원래 일반 Claude(디바이스 연결 기능이 있는 환경)를 기준으로 작성됐다.
Claude Code 세션에서 실행할 때는 아래처럼 대체한다:
- `device_list_dir` / `device_stage_files` → 불필요. Claude Code는 프로젝트 폴더에
  Read/Bash로 직접 접근 가능하므로 devlog.md를 바로 Read하면 된다.
- `mcp__remote-devices__Claude_Browser__*` → `mcp__claude-in-chrome__*` (사용자의
  실제 Chrome, 로그인 세션 유지됨)를 대신 쓴다.

## Velog 에디터를 실제로 조작하는 방법
velog.io를 브라우저 자동화로 조작할 때 필요한 URL 패턴, CodeMirror API,
이미지 업로드(클립보드 붙여넣기), `<details>` 토글 미지원, 저장/발행 2단계
확인 흐름, 시리즈 설정 UI 등 실전에서 확인한 세부사항은
`references/velog-editor-guide.md`를 읽는다. 여기 없는 새로운 사실을
알아내면 이 파일이 아니라 그 reference 파일에 추가한다 (본문은 짧게 유지).

## 스킬 이름/등록 정보 관련 알려진 문제와 해결 (2026-09-13)
이 Skill의 원래 이름은 `velog-weekly-devlog`였다. 범위가 넓어져서(devlog
발행 외에 History/Skill 소개 글 관리까지) 이름을 바꾸려고 처음엔
`velog-for-claude`로 정하고 frontmatter `name`, 폴더명, GitHub 저장소명을
전부 손으로 고쳤는데, **몇 번이고 폴더가 다시 `velog-weekly-devlog`로
복원되고 SKILL.md 수정 내용까지 전부 원본으로 되돌아가는 사고를 겪었다**
(GitHub 저장소 커밋 이력에서 매번 복구함).

한참 뒤에야 진짜 원인을 찾았다: **Claude 앱은 스킬 이름에 예약어 "claude"가
들어가는 걸 거부한다** ("Skill name in SKILL.md cannot contain the reserved
word 'claude'" — skill-creator의 패키징 UI로 업로드를 시도할 때 이 에러가
떴다). `velog-for-claude`라는 이름 자체가 애초에 유효하지 않았던 것이고,
그래서 파일 시스템을 아무리 손으로 고쳐도 앱이 계속 원래 상태로 되돌렸던
것이다 (수동 파일/폴더 편집이 문제가 아니라, 이름 자체가 등록될 수 없는
이름이었다). 최종적으로 `velog-publisher`로 이름을 바꿔서 해결했다.

**교훈**:
- 새 Skill 이름을 정할 때 "claude"라는 단어(대소문자 무관)가 들어가지
  않는지 먼저 확인한다.
- Skill 폴더 이름이나 SKILL.md의 `name`을 바꾸는 것만으로는 앱에 실제로
  반영되지 않는다 — 폴더/manifest.json을 직접 손으로 고쳐도 앱이 자체
  스킬 등록 정보와 다시 동기화하면서 되돌린다. **이름을 실제로 바꾸려면
  skill-creator의 `package_skill.py`로 새 `.skill` 파일을 패키징해서
  사용자가 앱의 스킬 업로드 화면에서 직접 "저장"해야 한다** (업로드 시
  이름 유효성도 그 자리에서 검사해준다).
- 내용만 바뀌는 경우(SKILL.md 본문, references/)는 로컬 파일을 편집해도
  되지만, **작업이 끝날 때마다 이 스킬 폴더를 GitHub 저장소
  ([muggie1379/velog-publisher](https://github.com/muggie1379/velog-publisher))에
  커밋/푸시해서 정본을 지킨다** — 앱이 예기치 않게 폴더를 되돌리는 경우가
  실제로 있었으므로, 로컬 파일만 믿지 않는다. 되돌아간 걸 발견하면 이
  저장소에서 `git clone`으로 복구한다.

## 실패 처리
- 폴더 미연결, devlog.md 새 내용 없음, velog 로그인 안 됨, 브라우저 조작 실패(UI 변경 등) 중 하나라도 발생하면 억지로 진행하지 말고 사용자에게 상황을 알린다 (예: send_later 나 다음 대화에서 보고)
- 이 스킬은 게시 직전 사용자 확인(위 "작업 마무리 루틴" 2번)을 반드시 거치므로, 그 확인 없이 바로 게시 버튼을 누르지 않는다

## 하지 말 것
- git 명령을 화면 자동화(터미널 열어서 타이핑)로 실행해 로그를 읽으려 시도하지 않는다 — 불안정해서 devlog.md 방식으로 대체하기로 합의됨
- devlog.md에 없는 성과/진행 상황을 창작하지 않는다
- 이 Skill의 이름을 폴더명/manifest.json 직접 수정만으로 바꾸려 하지 않는다 — 앱의 스킬 업로드 화면에서 `.skill` 패키지로 다시 저장해야 실제로 반영된다 (위 "스킬 이름/등록 정보 관련 알려진 문제와 해결" 참고). 이름에 "claude"라는 단어를 쓰지 않는다 (예약어라 앱이 거부한다).
